type Habit {
  id: Int!
  title: String!
  content: String!
  startAt: Date!
  ownerId: Int!
  owner: User!
  records: [Record]
  streakDays: Int!
  bestStreakDays: Int!
  createdAt: DateTime!
  updatedAt: DateTime
}
